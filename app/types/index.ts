export type Specification = {
  material: string;
  color: string;
  height: string;
  pattern: string;
  density: number;
  quantity: number;
  wall: number;
  explaination: string;
};

export type Color = {
    name: string,
    class: string
}